# In-person meeting — evidence pack (2026-06-18)

Generated by running `./tools/in_person_demo.sh` end-to-end from the main checkout
(`~/Dev/CAD/projects/TOTaLi`). Artifacts live under `$IN_PERSON_OUT`
(`~/Dev/data/in_person_demo`, gitignored). This pack records the verifiable
evidence to **show** at the meeting; the partner/PLS decisions captured during
the walkthrough go in `MEETING_CAPTURE_SHEET_2026-06-18.md`.

## 1. Demo ran clean — three DXFs + audit paths

| Track | Input | DXF output | Audit log |
|-------|-------|-----------|-----------|
| 1 — Coded survey | BV_BASE Carlson `.asc` | `…/bv_base_coded/totali_draft_output.dxf` | `…/bv_base_coded/audit/INPERSON-BVBASE_*.jsonl` |
| 2 — Real LAS | USGS tile (gates active, CRS inference) | `…/usgs_las/totali_draft_output.dxf` | `…/audit_las/INPERSON-LAS_*.jsonl` |
| 3 — Sales Key Flow | synth_topo.las (7-step narrative) | `…/sales_keyflow/totali_draft_output.dxf` | `…/sales_keyflow/audit/INPERSON-SALES_*.jsonl` |

Sales Key Flow ran all 7 steps; `total_duration_sec ≈ 0.70` vs `manual_baseline_sec = 14400` (4 h) — speed/provenance step printed `audit_verify=PASS`.

## 2. Audit chain verifies — `verify_log` PASS on all three

```
BV coded    verify_log -> PASS  (INPERSON-BVBASE_*.jsonl)
USGS LAS    verify_log -> PASS  (INPERSON-LAS_*.jsonl)
Sales flow  verify_log -> PASS  (INPERSON-SALES_*.jsonl)
```
The append-only SHA-256 audit chain (`totali/audit/verify.py::verify_log`) validates clean for every track — tamper/truncation would fail it.

## 3. DRAFT-only layers — no certified geometry emitted

Every `TOTaLi-*` layer across all three DXFs is `*-DRAFT` or a `QA` layer. **Zero certified (non-DRAFT, non-QA) layers leaked** — AI output stays on DRAFT until a PLS accepts/rejects.

- **USGS LAS / Sales flow (10 layers):** `SURV-DTM/BRKLN/CONT-MINOR/CONT-INDEX-DRAFT`, `PLAN-BLDG/CURB/HDSC/WIRE-DRAFT`, `QA-OCCLUSION`, `QA-FLAGS`.
- **BV coded (14 layers):** the above **plus** `SURV-MON-DRAFT`, `SURV-TOPO-DRAFT`, `SURV-WATER-DRAFT`, `PLAN-UTILITY-DRAFT` — the coded-survey field-code path maps to more layers than the LAS path.

> ⚠️ **Discrepancy to confirm with partner (feeds DXF spec §8):** the coded path emits **14** layers but `DXF_DELIVERABLE_SPEC.md` documents **10** (`cad_shielding.layer_mapping`). The 4 extra (MON, TOPO, WATER, UTILITY) come from field-code → layer mapping, not `layer_mapping`. The spec must cover both sources.

## 4. Invariants visible in config

`config/pipeline_in_person.yaml`:
```
auto_promote: false
require_pls_signature: true
```
Sales demo summary also prints `classification_authoritative: False`.

## 5. Pre-converted civil DWG → DXF (LibreDWG reference ONLY — not pipeline output)

Produced offline via the vendored LibreDWG `dwg2dxf` (v0.13.4), under `~/Dev/data/survey/converted/`:
- `110311 WATER BASE.dxf` (16 MB)
- `21053 - WATERLINE PTS CREATION.dxf` (64 MB)
- `XRF23173-Civil2013 EXP.dxf` (10 MB)

These demonstrate the DWG read path (DP-1) for reference; they are **not** TOTaLi pipeline output and carry no DRAFT/audit guarantees.

## 6. Talking points
- AI classifies (advisory) → algorithms measure (deterministic) → **human PLS certifies** (sovereign). Nothing is auto-promoted.
- The audit chain is independently verifiable (`verify_log`) — defensibility is mechanical, not trust-me.
- USGS tile is a **mechanical stand-in**; true U4 needs the partner's job LAS (still pending — see `SESSION_HANDOFF_2026-06-17.md §9`).
