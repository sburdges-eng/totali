"""Quarantine UI — operator-resolved CRS ambiguity gate.

Exposes the Flask `app` plus the module-level queue + audit-logger hook via
`totali.quarantine_ui.app`. See `AGENTIC.md` for the full Q-1..Q-6 spec.
"""
